# Copy to .env and edit for your setup

# Mac Studio hostname (mDNS)
FORGED_HOST=lumina.local

# HTTP server port
FORGED_PORT=8070

# Target machine SSH hostname
TARGET_HOST=hotcell-bench

# Target machine MAC address (for Wake-on-LAN)
TARGET_MAC=AA:BB:CC:DD:EE:FF
